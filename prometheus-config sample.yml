global:
  scrape_interval: 4s  

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: 
          - "localhost:8000"
          - "localhost:8001"
          - "localhost:8002"  
          
